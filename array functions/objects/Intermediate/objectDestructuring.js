const user = { 
    name: "Vijaya", 
    age: 24, 
    email: "vijaya@charmee.com" 
};

//Use destructuring to extract the name and email properties into variables.

const {name, email} = user;

console.log(name);
console.log(email);