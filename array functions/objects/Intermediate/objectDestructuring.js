const user = { 
    name: "Vijaya", 
    age: 24, 
    email: "vijaya@charmee.com" 
};

//Use destructuring to extract the name and email properties into variables.

const {name, email} = user;

console.log(name);
console.log(email);
console.log(user.age);

//objectDestructuring in JavaScript is used to extract values from objects and assign them to variables in a concise way. It makes your code cleaner and easier to read.


